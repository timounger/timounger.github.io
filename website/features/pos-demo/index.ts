/**
 * Public entry point of the POS demo feature.
 *
 * @remarks
 * Re-exports the {@link PosDemo} component so that consumers can import it from
 * a single module. Domain types are imported directly from "./types".
 *
 * @module
 */
export { default as PosDemo } from "./components/pos-demo";
